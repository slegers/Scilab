function [titel] = solve(x)
    // Decodeer Vigenere uit cypher.txt.
    // Geef titel van elke input.
    // 
    // select x
    // case 0 then titel = "dummy"
    // case 1 then titel = "titel input 1"
    // case 2 then titel = "titel input 2"
    // case 3 then titel = "titel input 3"
    // end

    // Dummy toekenningen aan outputvariabelen
    titel = -1
endfunction
